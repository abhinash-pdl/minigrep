# minigrep
A minimal grep-like CLI tool written in Rust.

## Install
```cargo install --git https://github.com/abhinash-pdl/minigrep```

## Usage
```minigrep <query> <file>```

```minigrep <query> <file> --ignore-case```

## Example
```minigrep "fn" "src/main.rs"```
